import styled from '@emotion/styled'

export const Wrapper = styled.div({
    display: 'flex',
    flexDirection: 'column',
    minHeight: '100vh',
    minWidth: '100vw',
    position: 'absolute',
    top: 0,
    left: 0,
})
